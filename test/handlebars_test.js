/**
 * Test case for handlebars.
 * Runs with mocha.
 */
'use strict'

const handlebars = require('../lib/handlebars.js')
const assert = require('assert')
const co = require('co')

describe('handlebars', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Handlebars', () => co(function * () {

  }))
})

/* global describe, before, after, it */
